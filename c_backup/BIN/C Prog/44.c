#include<stdio.h>
int main()
{
	int i,j,k,max;
	printf("Enter Three Numbers:");
	scanf("%d %d %d", &i, &i, &i);
	max=i;
	if(max<j)
		max=j;
	if(max<k)
		max=k;	
	printf("The Largest Number Is: %d",max);
	return 0;
}
