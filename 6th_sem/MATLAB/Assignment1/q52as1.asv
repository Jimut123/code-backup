%calculate tan(x) = sin(x)/cos(x) where x=0 to 360 in step of 5.

clc;
clear all;
close all;
for i=0:5:360
    y = tangent(i)
    fprintf('sin(%d)= %f\tcos(%d)= %f\ttan(%d) = %f',i, sine(i), i, cosine(i), i, y);
end
